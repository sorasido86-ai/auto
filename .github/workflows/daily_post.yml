name: run-daily-post

on:
  workflow_dispatch:
  schedule:
    - cron: "2-59/5 * * * *"   # ✅ 테스트: 5분마다 (UTC 기준, 00분 혼잡 회피)

jobs:
  post:
    runs-on: ubuntu-latest
    concurrency:
      group: run-daily-post
      cancel-in-progress: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Create bot_config.json from Secrets
        env:
          WP_BASE_URL: ${{ secrets.WP_BASE_URL }}
          WP_USER: ${{ secrets.WP_USER }}
          WP_APP_PASS: ${{ secrets.WP_APP_PASS }}
          KAKAO_REST_KEY: ${{ secrets.KAKAO_REST_KEY }}
          KAKAO_REFRESH_TOKEN: ${{ secrets.KAKAO_REFRESH_TOKEN }}
        run: |
          python - <<'PY'
          import os, json, sys

          required = ["WP_BASE_URL", "WP_USER", "WP_APP_PASS"]
          missing = [k for k in required if not os.getenv(k)]
          if missing:
            sys.exit("Missing secrets: " + ", ".join(missing))

          cfg = {
            "wp_base_url": os.environ["WP_BASE_URL"],
            "wp_user": os.environ["WP_USER"],
            "wp_app_pass": os.environ["WP_APP_PASS"],
          }

          # 카카오는 선택(Secrets 비워두면 스킵)
          if os.getenv("KAKAO_REST_KEY") and os.getenv("KAKAO_REFRESH_TOKEN"):
            cfg["kakao_rest_key"] = os.environ["KAKAO_REST_KEY"]
            cfg["kakao_refresh_token"] = os.environ["KAKAO_REFRESH_TOKEN"]

          with open("bot_config.json","w",encoding="utf-8") as f:
            json.dump(cfg,f,ensure_ascii=False,indent=2)

          print("bot_config.json written")
          PY

      - name: Run (test mode: new draft every run)
        env:
          TZ: Asia/Seoul
          TEST_MODE: "1"
          RUN_TAG: ${{ github.run_id }}   # ✅ 매 실행마다 고유 값(새 글 생성용)
          WP_STATUS: "draft"              # ✅ 테스트는 초안으로
        run: |
          echo "event=${{ github.event_name }}"
          python -u daily_post.py
